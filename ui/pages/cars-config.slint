import { Button, LineEdit} from "std-widgets.slint";

export component CarsConfig inherits Rectangle {
  background: darkorange;
  Text {
    x: 0;
    y: 0;
    font-size: 20px;
    text: "Here comes Car Config";
    vertical-alignment: center;
    padding-left: 20px;
    color: white;
  }
}
