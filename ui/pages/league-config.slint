import { Button, LineEdit} from "std-widgets.slint";

export component GeneralLeagueConfig inherits Rectangle {
  background: darkorange;
  Text {
    x: 0;
    y: 0;
    font-size: 20px;
    text: "Here comes League Config";
    vertical-alignment: center;
    padding-left: 20px;
    color: white;
  }
}
