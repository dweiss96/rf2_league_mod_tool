import { Button, LineEdit} from "std-widgets.slint";

export component DriversConfig inherits Rectangle {
  background: darkorange;
  Text {
    x: 0;
    y: 0;
    font-size: 20px;
    text: "Here comes Drivers Config";
    vertical-alignment: center;
    padding-left: 20px;
    color: white;
  }
}
